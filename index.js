const Typium = new (class {
  #data;
  add(obj) {

    const isNameHasSpace
    if (obj.name) {
      if (obj.value) { }
      if (obj.type) {
        if (obj.value instanceof obj.type) {

        }
      }
    }
  }
}
  get _() {
  return this.#data;
}
}) ();
